import { DiretivaDirective } from './diretiva.directive';

describe('DiretivaDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaDirective();
    expect(directive).toBeTruthy();
  });
});
